import React from 'react'

import { Togle_button } from './styles'

const drawerToggleButton = props => (
  <Togle_button onClick={props.click}>
    <div/>
    <div/>
    <div/>
  </Togle_button>
)

export default drawerToggleButton